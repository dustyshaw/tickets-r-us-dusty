apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  namespace: dusty
  name: blazor-web-ingress
spec:
  ingressClassName: nginx
  rules:
  - host: dusty-kube.duckdns.org
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: blazor-web-ingress
            port:
              number: 80